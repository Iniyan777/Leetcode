//Specify the length of the returned array 
*returnSize=2; 
 int* result=(int *)malloc(sizeof(int)*2); 
 //Find the target value through a double-level loop
for(int i=0;i<numsSize;i++){
        for(int j=i+1;j<numsSize;j++)
        {
            if(nums[i]+nums[j]==target)
            {              
            result[0]=i;
            result[1]=j;    
            return result;
            }
        }  
}

free(result);   
return 0;
